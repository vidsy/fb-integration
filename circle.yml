general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker
  environment:
    PATH_BASE: "/go/src/github.com/vidsy"
    GO_BUILDER_IMAGE: "vidsyhq/go-builder"

dependencies:
  override:
    - make install-dependencies

test:
  override:
    - make test_ci
    - make check-version
