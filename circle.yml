general:
  branches:
    ignore:
      - master

machine:
  services:
    - docker

dependencies:
  override:
    - make install-dependencies

test:
  override:
    - make build-ci
    - make test-ci
    - make vet-ci
    - make check-version